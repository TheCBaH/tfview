name: OCaml Build

on:
  push:
  pull_request:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: checkout
      uses: actions/checkout@v6
      with:
        submodules: recursive
    - name: devcontainer
      uses: ./.github/workflows/actions/devcontainer
      id: devcontainer
    - name: setup
      run: |
        echo 'EXEC=${{ steps.devcontainer.outputs.exec }}' >> $GITHUB_ENV
    - name: build flatc
      run: |
        ${{ env.EXEC }} make flatc
    - name: install ocaml deps
      run: |
        ${{ env.EXEC }} bash -c '. /etc/bash.bashrc && sudo OPAMROOT=$OPAMROOT make deps'
    - name: check generated schema
      run: |
        ${{ env.EXEC }} make generate-check
    - name: check formatting
      run: |
        ${{ env.EXEC }} make fmt-check
    - name: build tfview
      run: |
        ${{ env.EXEC }} make build
    - name: download test model
      run: |
        ${{ env.EXEC }} make download
    - name: run tfview
      run: |
        ${{ env.EXEC }} make run
    - name: test jsoo target
      run: |
        ${{ env.EXEC }} make test-jsoo
    - name: build web-jsoo target
      run: |
        ${{ env.EXEC }} make build-web-jsoo
